import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.awabcodes.purrworld',
  appName: 'Purr World',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
