
export class Source {
    constructor(
        public id?: string, // required
        public name?: string, // required
        public website_url?: string, // required
        public breed_id?: string,
    ) {
    }
}